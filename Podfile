# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def global_pods
  pod 'RxSwift', '~> 4.3.1'
end
target 'Core' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  global_pods
  pod 'RxRealm'
  # Pods for Core

end

target 'Domain' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  global_pods
  # Pods for Domain

end

target 'ReactiveCleanArchitecture-Example' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  global_pods
  pod 'RxCocoa'
  # Pods for ReactiveCleanArchitecture-Example

end
